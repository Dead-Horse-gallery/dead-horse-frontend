'use client';

import DeadHorseDemo from '@/components/DeadHorseDemo';

/**
 * Comprehensive Demo Page for Dead Horse Gallery Authentication System
 * 
 * This replaces all individual test pages:
 * - /test-protected-demo (ProtectedActionDemo)
 * - /test-protected-action (ProtectedActionExample) 
 * - /test-auth-modal (AuthModal demo)
 * - /test-hybrid-auth (AuthDemo)
 * - /test-auth (Basic auth test)
 * - /test-protected (Basic protected test)
 * 
 * Everything is consolidated into a single, comprehensive demo.
 */
export default function ComprehensiveDemoPage() {
  return <DeadHorseDemo />;
}
