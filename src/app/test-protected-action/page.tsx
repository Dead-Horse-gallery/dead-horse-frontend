'use client';

// Update the import path below to the correct relative path if the file exists elsewhere, for example:
import ProtectedActionExample from '../../components/ProtectedActionExample';
// Or ensure that the file 'ProtectedActionExample.tsx' exists in 'src/components'

export default function TestProtectedActionPage() {
  return <ProtectedActionExample />;
}
